interface IConstants {
  FALLBACK_PORT: number;
  SERVER_START_MSG: string;
}

const constants: IConstants = {
  FALLBACK_PORT: 3000,
  SERVER_START_MSG: 'Express server started on port: ',
};

export default constants;
